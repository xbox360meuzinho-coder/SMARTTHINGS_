# Personal Access Token do SmartThings
# Gere em: https://account.smartthings.com/tokens
# Escopos necessários: r:devices:*, x:devices:*, r:locations:*
ST_PAT=seu_personal_access_token_aqui

# Porta (Render define automaticamente via process.env.PORT, isso é só fallback local)
PORT=3000
